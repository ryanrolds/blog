# Blog

## Setup

Requires PostgreSQL and PG user with CREATEDB priv when running tests.

    $ export PG_URL=postgres://<user>:<pass>@<host>/<db>
    $ npm install


## TODO

* Store posts in DB
* Track views, likes, shares
* Add user auth
